spring:
  application:
    name: BookingMS
  cloud:
    consul:
      discovery:
        hostname: localhost
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/booking_db
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update

server:
  port: 8900
