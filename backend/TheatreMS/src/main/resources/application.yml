spring:
  application:
    name: TheatreMS
  cloud:
    consul:
      discovery:
        hostname: localhost
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/theatre_db
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update

server:
  port: 8800
